# Application Set-up (vibe code)

1. The current project structure and key components we've built:

   - Navigation system with collapsible sidebar
   - TopBar with user menu
   - Notification system (Toast and NotificationList)
   - Theme management
   - Redux store setup

2. The main features we've implemented:

   - Drawer open/close functionality
   - Random notification generation
   - Sticky vs non-sticky notifications
   - Theme switching capability
   - Menu interactions

3. The technologies we're using:

   - Next.js
   - Material-UI (MUI)
   - Redux Toolkit
   - TypeScript

4. The current configuration files:
   - `next.config.ts` (with static export settings)
   - Any existing test setup files
